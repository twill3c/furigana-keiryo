# SPEC.md — furigana-keiryo（振仮名計量）

<!-- scaffold template v1.12.0 から展開(2026-08-27)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

青空文庫の振り仮名注記を解析し、作家の振り仮名の振り方を**二つの独立した軸**で計量する。

- **第一軸 総ルビ傾向** — そもそもどれだけ振るか
- **第二軸 難所への偏り** — 自分の傾向から予測されるより、難読字に偏って振るか（優しさの残差）
- **第三軸 繰り返しの抑制** — 自分の傾向から予測されるより、再出現で振り直さずに済ますか（記憶の残差）

### 二軸を捨てて三軸にした経緯（2026-08-27 / loop_002・loop_003 実測）

第二軸の候補を二つ立て、**二つとも支配因子の言い換えに潰れた**。

| 候補 | 総ルビ傾向との相関 | 判定 |
|---|---|---|
| 信用（易読帯の非ルビ率） | r = −0.994 | G-04 失格 |
| 記憶への信用（反復率の裏返し） | r = −0.813 | G-10 失格 |

どちらも設計時には独立だと論証していた。誤りは共通していて、
**母集団の構成**についての論証を、**分散の出所**についての論証と取り違えていた。
互いに素でも片方が全体の質量を占めれば潰れ、母集団を限定しても支配因子はその中で働く。

偏相関を取ると `優しさ × 記憶 | 総ルビ傾向 = −0.093` であり、実際の構造は

> **支配的な 1 次元 ＋ 互いに独立な弱い残差 2 つ**

だった。よって支配因子と直交する軸を探すのをやめ、**支配因子を第一軸として名指しし、
残りを明示的な残差として定義する**。HC-039 に起票。

### 旧・第二軸を差し替えた経緯（2026-08-27 / loop_002 実測）

当初の第二軸は「信用（放任率）＝易読漢字のうちルビを振らなかった割合」だった。
**実測の結果これは第二の軸ではなく、総ルビ傾向の言い換えだった**（`r = −0.993`）。
易読帯は定義上、全漢字出現の 75% を占めるため、そこで測った率は全体の率に潰れる。
**母集団が互いに素であることは足りない。帯の質量が均衡している必要がある**（HC-025 と同型）。
経緯は `FINDINGS.md` に残す。

差し替えた第二軸は、**統計的な手当てではなく定義によって**総ルビ傾向から独立する。
母集団を「その作家が振ると決めた語の、二度目以降の出現」に限るためである。
どれだけ振るか（傾向）と、振った語を繰り返すか（反復）は、別々に選べる。

解析は Ruby で行う。青空文庫の注記は独自記法（`｜`・`《》`・`※［＃…］`）の入れ子であり、
日本語の文字種プロパティ（`\p{Han}` / `\p{Hiragana}` / `\p{Katakana}`）を素で扱える Onigmo と、
UTF-8 を文字単位で操作する Ruby の文字列処理が最も直接に対応する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 青空記法を解析木にする。ルビ（`《》`）・ルビ始点（`｜`）・入力注記（`※［＃…］`）・傍点を区別する | must |
| F-02 | 解析木から原文を**一字も違わずに**組み直せる（往復検査の片道） | must |
| F-03 | 本文の凡例・奥付・注記ブロックを除去し、計量対象の本文だけを切り出す | must |
| F-04 | 漢字の難読度を**コーパス出現頻度**から決める。作家一人抜き（leave-one-author-out）で作る | must |
| F-05 | ルビを三分類する — 救済（難読字へ）／過剰（易読字へ）／義務（読みが一意に定まらない） | must |
| F-06 | 作家ごとに優しさ（救済率）・信用（放任率）を算出する。母集団は互いに素 | must |
| F-07 | 二軸を散布させ、四象限（過保護／案内人／ちぐはぐ／突き放し）の分布を出す | must |
| F-08 | `文字遣い種別`（旧字旧仮名／新字新仮名）と刊行年代を共変量として併記する | should |
| F-09 | 一作品の本文上で、各ルビがどちらの軸に効いているかを色分けして見せる | should |
| F-10 | 該当作を収録しているフリート内アトラスへ導線を張る | could |
| F-11 | 反復率を測る。作品内で一度以上ルビが振られた基底語について、**2 回目以降の出現**のうちルビが再度振られた割合 | must |
| F-12 | 優しさ・記憶を総ルビ傾向へ回帰し、**残差**を第二軸・第三軸とする。回帰は最小二乗・同一標本で行う | must |
| F-13 | 総ルビ組版の作品を**作品単位**で検出し、母集団から分離する。検出は易読帯ルビ率 `p_easy` による | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 解析・計量ロジックは全て Ruby。JS は表示とグルーのみ | 計量を行う JS がないことを検査 |
| N-02 | 本文の再取得を行わない。`aozora-sakuin/data/raw` の既取得分のみを使う | pipeline に HTTP クライアントが無いことを検査 |
| N-03 | 標準ライブラリのみで動く。gem のネイティブビルドに依存しない | `bundle` 無しで `ruby test/run.rb` が通る |
| N-04 | Vercel 静的配信のみ。サーバ側処理と運用コストを持たない | vercel.json に framework なし |
| N-05 | 収録の部分性（作家あたり最大 17 作・選定規則）を画面に明示する | 目視 + テスト |

## 4. 品質基準

### オラクル（各ループで必ず回す）

| ID | 検査 | 基準 |
|---|---|---|
| O-01 | 往復検査：`rebuild(parse(src)) == src` | 全 6,195 作で**完全一致**。1 件でも欠ければ失格 |
| O-02 | 二軸の独立：母集団が互いに素 | 救済の母集団 ∩ 過剰の母集団 = ∅ を集合演算で検査 |
| O-03 | 難読度の非循環：頻度表がルビ情報を参照しない | 頻度表の構築入力に本文（ルビ除去後）以外が無いことを検査 |
| O-04 | 一人抜きの実効性：作家 A の頻度表に A の本文が入らない | A を含む表と含まない表で値が変わることを検査 |
| O-05 | 決定論：再実行で出力 JSON の sha256 が不変 | 2 回実行して比較 |

### 較正ゲート

| ID | 検査 | 状態の意味 |
|---|---|---|
| G-01 | O-01 往復検査が全作品で一致 | 不一致があればパーサは未完成。以後の計量値は無効 |
| G-02 | 未知の記法パターンが 0 件 | 未知があれば取りこぼしの可能性。件数と実例を必ず出す |
| G-03 | 外字注記（面区点番号）の写像率 | 写像不能を握りつぶさない。件数を出す |
| G-04 | 二軸の相関 \|r\| < 0.7 | これを超えたら二軸は実質一軸。分ける意味が無い（**設計の失格条件**） |
| G-05 | 循環の禁止（O-03） | 難読度をルビ分布から作っていないこと |
| G-06 | 難読度の時代整合 | 頻度表の刊行年代帯が対象作と揃っていること |
| G-07 | 義務ルビの分離 | 熟字訓・当て字・特殊読みを両軸から外せているか |
| G-08 | 底本差によるルビ揺れ | 同一作品の異版でルビ密度が動く分を測り、軸の差より小さいことを確認 |

### 第二軸のゲート（loop_003 で新設・**測定前に宣言**）

| ID | 検査 | 判定線 |
|---|---|---|
| G-09 | 二軸の独立 | \|r(優しさ, 記憶への信用)\| < 0.7 |
| G-10 | 交絡からの独立 | \|r(記憶への信用, 総ルビ傾向)\| < 0.5 |

**G-10 が今回の要である。** loop_002 で第二軸を殺したのは総ルビ傾向との相関（−0.993）であり、
G-04 だけでは同じ失敗を繰り返す。0.5 という線は、実測を見る前に引いた。
落ちたら軸を捨てる。線を動かして通すことはしない（HC-030）。

初出の除外も判定前に決めた規則である。初出にルビを振るのは慣行なので、
初出を分母に含めると全作家が同じ値に寄り、軸が潰れる。

### 三軸のゲート（loop_004 で新設・**測定前に宣言**）

| ID | 検査 | 判定線 |
|---|---|---|
| G-11 | 残差の再現性（半分割） | 各残差軸について r(前半, 後半) > 0.5 |
| G-12 | 残差同士の独立 | \|r(難所への偏り, 繰り返しの抑制)\| < 0.3 |
| G-13 | 帰無分布からの分離 | 置換検定（作家ラベル 2,000 回・seed 20260827）で、観測の半分割相関が帰無分布の最大値を超えること |

**残差が支配因子と直交するのは回帰の構成上あたりまえであり、成果ではない。**
したがって G-10 型の検査は新設計では意味を持たない。代わりに問うべきは
「その残差は作家の性質か、標本の雑音か」である。

- **G-11 が要である。** 作家の作品を前半・後半に二分し、それぞれ独立に残差を出して
  作家間で相関させる。残差が作家の性質なら高く、雑音なら 0 に落ちる
- **G-13 は G-11 の基準線を実測で引く。** 0.5 という数字自体に根拠はないので、
  作家ラベルを混ぜた帰無分布と比べて、観測がそこから外れていることを別に示す

半分割は作家ごとに作品 ID 昇順で交互に振り分ける（決定論・O-05）。
判定線はいずれも実測を見る前に引いた。落ちたら三軸も捨てる。線は動かさない（HC-030）。

### 総ルビ分離のゲート（loop_005 で新設・**測定前に宣言**）

総ルビ組版（本文の全漢字にルビを振る組版）は、作家の判断ではなく**底本の組版方針**である。
第一軸がそういう作品について測っているのは編集判断であって文体ではない。

検出は密度ではなく**難易度への非依存**で行う。総ルビの署名は
「易読帯の漢字にまでルビが振られる」ことである。誰でも読める字に毎回振る作家はいない。
よって作品ごとの **`p_easy` ＝ 易読帯漢字のうちルビが振られた割合** を見る。

| ID | 検査 | 判定線 |
|---|---|---|
| G-14 | 総ルビは**別の母集団**である | `p_easy` の分布が二峰性を示す。区間 `[0.5, 0.9)`（谷）の作品数が、両側 `[0.1, 0.5)` と `[0.9, 1.0]` の**どちらよりも少ない**こと |
| G-15 | 総ルビは**文体ではなく組版方針**である | 総ルビ作を持つ作家のうち、**非総ルビ作も持つ作家が過半数**（> 50%）であること |
| G-16 | 分離後も三軸が立つ | 総ルビ作を除いて再測しても G-11 / G-12 / G-13 がすべて通ること |

**G-14 と G-15 は、分離してよいかどうかを問うゲートである。**

- G-14 が落ちたら、総ルビは連続的な程度問題であって別の母集団ではない。**分離してはならない**
- G-15 が落ちたら、総ルビは作家ごとに一貫した性質＝**文体**であり、
  「組版方針だから除く」という理屈が成り立たない。**分離してはならない**

いずれの場合も、除外せずに「総ルビ度」を共変量として併記する方針に切り替える。
分離ありきで進めない。判定線は実測を見る前に引いた。

**G-04 は本プロジェクトの失格条件である。** 二軸が一直線に並ぶなら、
「優しさ」と「信用」は同じ量の言い換えでしかなく、この計量は成立しない。
その場合は結果を捨て、一軸に畳んだ上で企画を見直す。

## 5. スコープ外

- **本文の再取得**。`aozora-sakuin` の既取得分のみを使う（N-02）
- **形態素解析**。ルビは基底文字列が注記で明示されているため、分かち書きを必要としない
- **読みの正誤判定**。「そのルビが正しい読みか」は問わない。振られたか否かだけを見る
- **現代文への適用**。対象は青空文庫収録作（著作権保護期間満了）に限る
- **作家の意図の推定**。測るのは表記の分布であって、作家が何を考えていたかではない。
  「優しさ」「信用」は**指標につけた名前**であり、心理の測定ではない
- **底本の校訂**。青空文庫のテキストをそのまま正とする

## 6. 前提とその危うさ

| 前提 | 危うさ | 対処 |
|---|---|---|
| 出現頻度が難読度の代理になる | 頻度が低くても文脈で読める字がある（`薔薇`） | 分位で粗く帯を切り、細かい順位差を解釈しない |
| コーパスが同時代を代表する | 青空文庫は文学に偏る。新聞・実用書を含まない | 母集団の偏りを画面に明示する（N-05） |
| ルビの有無が作家の判断を反映する | 底本・編集者・出版社の方針が混ざる | G-08 で異版差を測り、軸の差と比べる |
| 作家あたり最大 17 作で足りる | 多作家の全体像は取れない | 均衡設計として受け入れ、作品数を併記する |
